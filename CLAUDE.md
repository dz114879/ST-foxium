# ST-foxium (FFSS) 维护指南

面向 SillyTavern 的交互式 Bash 维护工具，提供修复 / 编辑 / 优化功能；运行在用户的 Termux、Linux、Windows Git Bash 环境，直接操作用户的 ST 安装。

## 通用原则

- **从第一性原理出发。** 先确认开发者真正要的结果、平台条件、已有外部契约（ST 当前版本的源码结构、脚本对用户文件的改动、已发布版本的行为）和证据强度，再选择技术与改动位置；原则发生张力时回到原始目标和证据说明取舍，新事实推翻旧判断时修改设计。
- **通用大于特判。** 先判断多个需求是否共享同一语义或不变量，优先扩展共同机制（`lib/common.sh` 的工具函数、统一的备份与确认流程）；真实差异清楚保留，没有共同需求时不提前建立万能抽象。
- **Fail fast，避免静默降级。** 约束失效或结果无法确认时停止相关操作，并传播带上下文的错误；预期内的恢复路径必须语义明确且对用户可见，未知状态不猜测成功结果。
- **可维护性优先。** 代码和文档应让后来的维护者无需依赖隐含历史即可理解、验证和修改：功能按模块放进 `lib/`，`main.sh` 只做启动检查与菜单调度；责任、数据流和错误路径保持清楚，新增复杂度必须有现实问题支撑。
- **KISS 与 60/95。** 选择能完整解决当前问题的最简单方案，用 60% 的复杂度覆盖 95% 的现实场景，罕见环境组合留到真实遇到时处理；不预置假想场景的兜底。不得借此省略已有契约（改文件前备份、危险操作确认、版本判断）。
- **先查证再下结论。** 涉及 SillyTavern 官方实现与版本行为、jq/yq 方言差异、npm/node 生态或 Termux/Git Bash 环境行为时，先查一手资料或实际运行验证，不要凭记忆下判断。
- **遵循，但不拘泥。** 原则之间可能产生张力，遇到冲突时回到原始目标和已有证据，说明取舍，用实践判断选择当前合适的方案。

## 项目硬约定

- 任何会修改文件的动作，必须先 `create_backup`，再执行修改；这是对用户数据的既有承诺。
- `build/ffss.sh` 是发布用单文件产物（`main.sh` 与全部 `lib/` 的拼接），README 的安装命令直接下载它；改完 `lib/` 必须重新生成它，否则用户拿不到新代码。
- 面向 bash 4+ 编写，保持 Termux / Linux / Git Bash 三平台可用；路径处理一律加引号，不假定有 root 或 systemd。

## 回答风格

优先拆解开发者需求；补充信息、理论推导和策略选择只在直接服务于具体问题时引入，排除无关炫学。纠正事实时引用开发者原文并指出具体错误。回答篇幅匹配问题复杂度，保持扁平结构；仅在至少三个并列项且存在明确键值关系时使用列表，不把短语单独列为项目。落盘文件中的文件树、架构图等层级信息使用缩进 YAML，而不是 ASCII；对话中允许使用 ASCII。

## Git

commit message 使用 Conventional Commits（`<type>[optional scope]: <description>`），沿用现有历史风格（`docs: …`、`feat!: V2`）。
